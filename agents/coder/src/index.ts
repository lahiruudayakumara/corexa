export const coderAgent = {
  role: "coder",
  responsibilities: [
    "apply workspace changes within sandbox constraints",
    "use runtime tools, shell, and editor abstractions",
    "emit structured patches, test results, and implementation notes",
  ],
} as const;
