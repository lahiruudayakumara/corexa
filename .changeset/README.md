# Changesets

Corexa uses Changesets to coordinate versioning for workspace packages and release automation.

Create a changeset when a package-facing change is introduced:

```bash
pnpm changeset
```
