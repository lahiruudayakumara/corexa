# Qdrant Collections

This directory reserves collection configuration and migration artifacts for Corexa memory stores.

Planned collections:

- `workspace_chunks`
- `workspace_symbols`
- `conversation_memory`
- `agent_runs`
- `workspace_facts`
